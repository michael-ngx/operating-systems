Created a VM that hosts a Debian machine

This machine has IP address: 192.168.64.6
- This is generated by logging into the VM, enter the command:


ip -br -4 addr

Then, to 'remote development' to the debian machine, you can do:

ssh 192.168.64.6

